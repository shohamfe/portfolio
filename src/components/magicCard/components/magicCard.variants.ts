export const magicCardRoot = "relative isolate";

/** First child, so it paints above the card background but under everything
 *  the card renders - the glow only shows where the content lets it through. */
export const magicCardGlow = "pointer-events-none absolute inset-0";
